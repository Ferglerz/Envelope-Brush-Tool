-- js_ReaScript JS_Mouse_GetState indices and WM_MOUSEWHEEL wParam LOWORD masks (wheel on arrange).

return {
    JS_CTRL = 4,
    JS_CMD = 16,
    JS_SHIFT = 8,
    --- Alt / Option (not bit 32 — that is VK_SPACE in this API).
    VK_MENU = 0x12,
    MK_SHIFT = 0x0004,
    MK_CONTROL = 0x0008,
}
